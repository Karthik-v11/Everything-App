<?xml version="1.0" encoding="utf-8"?>
<!--
  Today's tasks (Requirement 13).

  A ListView, not fixed rows. Four pre-declared rows were cheaper, but they are
  laid out into whatever height the user resized the widget to and then clipped:
  a widget two cells tall with four tasks drew three and a half of them and gave
  no way to reach the fourth. A collection widget is the only thing the launcher
  will let scroll, so the list is bound through TodayTasksWidgetService.

  The ListView and the empty view are siblings in one FrameLayout because
  `setEmptyView` requires it — the launcher swaps between them by id, and a view
  in a different parent is simply never shown.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_background"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/widget_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:maxLines="1"
            android:ellipsize="end"
            android:textColor="@color/widget_text"
            android:textSize="15sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/widget_overdue"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:textColor="@color/widget_overdue"
            android:textSize="11sp"
            android:visibility="gone" />

        <TextView
            android:id="@+id/widget_add"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:paddingTop="2dp"
            android:paddingBottom="4dp"
            android:background="@drawable/widget_pill"
            android:text="@string/widget_add"
            android:contentDescription="@string/widget_add_task"
            android:textColor="@color/widget_accent"
            android:textSize="15sp" />
    </LinearLayout>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="8dp">

        <!-- No divider: the spacing between rows is the row's own padding, as
             it was when the rows were declared here. -->
        <ListView
            android:id="@+id/widget_task_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:divider="@null"
            android:dividerHeight="0dp" />

        <TextView
            android:id="@+id/widget_empty"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/widget_all_clear"
            android:textColor="@color/widget_text_dim"
            android:textSize="13sp" />
    </FrameLayout>

    <TextView
        android:id="@+id/widget_updated"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end"
        android:textColor="@color/widget_text_dim"
        android:textSize="10sp" />
</LinearLayout>
